<% layout("/layouts/boilerplate") %>
<h2>Your Wishlist</h2>

<% if(wishlist.length==0){%>
  <h3 style="text-align:center">Your list is empty</h3>
<%}%>
<!-- <ul>
  <% wishlist.forEach(listing => { %>
    <li>
      <a href="/listings/<%= listing._id %>">
        <img src="<%= listing.image.url %>" alt="<%= listing.title %>" style="height: 100px; width: 100px;">
        <span><%= listing.title %></span>
      </a>
    </li>
  <% }) %>
</ul> -->


<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
    <% for(let listing of wishlist) {%>
      <a href="/listings/<%=listing._id%>" class="listing-link">
        <div class="card col " >
            <img src="<%=listing.image.url%>" class="card-img-top" alt="image" style="height: 20rem">
            <div class="card-img-overlay"></div>
            <div class="card-body">
              <p class="card-text"><b><%=listing.title%></b>
              <br>&#8377; <%= listing.price.toLocaleString("en-IN") %>  /night
            <i class="tax-toggle"> &nbsp; &nbsp; +18% tax</i></p>
            </div>
          </div>
        </a>
        <%}%>
        </div>
